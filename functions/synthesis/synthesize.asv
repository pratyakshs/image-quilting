function [ out_im ] = synthesize( Im_inp, Im_target, alpha, patch_size, overlap_size, factor, no )
%SYNTHESIZE
out_im = loop_over_items(Im_inp, Im_target, alpha, patch_size, overlap_size, factor, [], 0);
for i = 2:no
  
    

end

